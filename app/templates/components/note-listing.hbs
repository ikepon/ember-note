<div class="col-xs-4">
  <ul class="notes">
    {{#each notes as |note|}}
      <li class="note-title">
        {{#link-to "notes.show" note}}
          {{note.title}}
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</div>
